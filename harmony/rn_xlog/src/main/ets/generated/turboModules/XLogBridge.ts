/**
 * This code was generated by "react-native codegen-lib-harmony"
 */

import { Tag } from "@rnoh/react-native-openharmony/ts"

export namespace XLogBridge {
  export const NAME = 'XLogBridge' as const

  export interface Spec {
    open(): Promise<void>;
  
    close(): Promise<void>;
  
    log(level: number, tag: string, message: string): Promise<void>;
  
    installUncaughtCrashHandler(): Promise<void>;
  
    uninstallUncaughtCrashHandler(): Promise<void>;
  
  }
}
